# batchsample

The samples are in there designed branch available.

* sample1: Contains a sample demonstrating usage of `@KafkaListener` in batch scenarios.
* sample2: Shows usage for Spring Cloud Stream functional programming model (not @KafkaListener, not Spring Integration annotations — just a plain Java java.util.function.Consumer). In both single and batch mode.

